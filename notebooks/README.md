# notebooks

데이터 확인, 일회성 분석, 아이디어 검증을 위한 Jupyter notebook을 두는 디렉터리입니다.

재현 가능한 전처리, 학습, 평가 로직은 notebook에만 남기지 않고 `src/`, `scripts/`, `experiments/`로 옮깁니다. notebook이 생성하는 대형 출력과 `.ipynb_checkpoints/`는 커밋하지 않습니다.
